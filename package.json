{
  "name": "application-name",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node app"
  },

  "dependencies": {
    "express": "*",
    "jade": "*",
    "canvas": "git://github.com/elspoono/node-canvas-heroku.git#master"
  },
  
  "devDependencies": {
    "canvas": "*",
    "supervisor": "*"
  },

  "engines": {
    "node": "0.8.x",
    "npm": "1.1.x"
  }
}
